{
  "name": "symphony-ts",
  "version": "1.0.0",
  "description": "TypeScript Symphony orchestrator engine (per SPEC.md)",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run src/main.ts",
    "build": "bun build src/main.ts --compile --outfile ../bin/symphony-ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "liquidjs": "^10.16.0",
    "yaml": "^2.6.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}
